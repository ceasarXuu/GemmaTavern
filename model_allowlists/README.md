# Model Allowlists

This directory contains the JSON allowlists that Gemma Tavern can fetch from GitHub to populate or refresh the public model catalog.

Guidelines:

- keep file names stable,
- do not include private URLs or credentials,
- update public docs if the allowlist format or delivery flow changes.